export type Club = {
    mal_id: number
    url: string
    images: {
        jpg: {
            image_url: string
        }
    }
    name: string
    members: number
    category: string
    created: string
    access: string
}
